const Aboutus = ({
  text,
  sub_text,
  image1,
  image2,
  image3,
  text_description,
  description,
}) => {
  const about = [
    { text, sub_text, id: 1 },
    { image1, image2, image3 },
    { text, sub_text, id: 2 },
    { text, sub_text, id: 3 },
    { text, sub_text, id: 4 },
  ];
  return (
    <div>
  
    </div>
  );
};

export default Aboutus;
