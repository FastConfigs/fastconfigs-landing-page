
const MainFileAbout = (props) => {
    return (
        <div className="about-us">
        <h2>{props.about_us}</h2>
        <p className='color-p-about'>{props.description}</p>
        </div>
    )
}

export default MainFileAbout
